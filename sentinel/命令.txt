// 将本地tar包添加到本地镜像
docker load -i openjdk-8-jdk-alpine.tar
// 查看镜像
docker images

docker build -t sentinel:1.8.8 .

// 正常启动
docker run -d -p 8081:8081 --name sentinel-1.8.8 sentinel:1.8.8

// 带自启动
docker run -d --restart=always -p 8081:8081 --name sentinel-1.8.8 sentinel:1.8.8